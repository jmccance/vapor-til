vapor-til
=========

Start the database:

```
docker-compose up -d
```

Build the app:

```
vapor build
```

Run the app:

```
vapor run
```

Prove it's working:

```
curl localhost:8080
```

```
It Works!
```
